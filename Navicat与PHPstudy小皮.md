### Navicat与PHPstudy小皮

navicat怎样新建连接my sql 打开本地的一个house.sql

**1. 创建到 MySQL 服务器的连接** 

1. **打开 Navicat**，点击左上角的 `连接` 按钮，然后在下拉菜单中选择 `MySQL`。
2. **填写连接信息**。会弹出一个名为“新建连接”的窗口，你需要填写以下关键信息：
   - **连接名（Connection Name）**: 这是一个标识符，你可以随意起一个容易记住的名字，比如 `本地MySQL`、`My_Local_Server`。
   - **主机（Host）**: 因为是本地数据库，所以填写 `localhost` 或 `127.0.0.1`。
   - **端口（Port）**: MySQL 默认端口是 `3306`，除非你修改过，否则保持默认即可。
   - **用户名（Username）**: 连接数据库的用户名，通常是 `root`。
   - **密码（Password）**: 安装 MySQL 时你为 `root` 用户设置的密码。(c058836)
3. **测试连接**。填写完毕后，点击左下角的 `测试连接` 按钮。如果弹出“连接成功”的提示，说明所有信息都正确。
4. **保存连接**。点击“确定”按钮，这个连接就会保存到 Navicat 的主界面中。

**2. 打开（执行） SQL 文件**

现在已经连接到了 MySQL 服务器，但还没有数据库。`house.sql` 文件通常是一个包含建库、建表、插入数据等 SQL 命令的脚本。需要创建一个空数据库，然后在这个数据库上执行这个脚本。

**方法一：先建库，再执行 SQL 文件（推荐）**

1. **新建一个数据库**：
   - 右键点击你刚刚创建的连接（例如 `本地MySQL`），选择 `新建数据库`。
   - 在弹出的窗口中，填写：
     - **数据库名**: 例如 `house_db`（名字可以自定，但最好和你的项目相关）。
     - **字符集**: 选择 `utf8` 或 `utf8mb4`（推荐 `utf8mb4`，支持更全面的字符，如表情符号）。
     - **排序规则**: 选择 `utf8mb4_general_ci` 或 `utf8_general_ci`。
   - 点击“确定”。
2. **打开并执行 SQL 文件**：
   - **双击**打开新建的 `house_db` 数据库，**图标变亮**，表示当前正在操作这个数据库。
   - 在数据库名上点击鼠标右键，选择【运行SQL文件】，找到对应文件开始运行，执行完成后，会有执行是否成功的提示，比如成功：Finished successfully.
   - 或：（以下好像不行）
   - 点击 Navicat 顶部的菜单栏中的 `文件` -> `打开外部文件`。
   - 在弹出的文件浏览器中找到你的 `house.sql` 文件并打开。文件内容会显示在一个新的查询选项卡中。
   - **重要**：确保你当前选中的数据库是 `house_db`（查看窗口左上角或左下角的状态）。
   - 点击查询窗口上方的 `运行` 按钮（一个三角形的“播放”图标）。
3. **查看结果**：执行完成后，右下角会有提示。右键点击 `house_db` 数据库下的“表”，选择“刷新”，就能看到从 `house.sql` 文件里创建的所有数据表了。



双击数据库（如 `house_db`）的主要作用**不是**直接打开或显示所有数据，而是**“选中”或“切换”当前活动的数据库**。

**如何判断双击是否成功？**
双击后，你会看到两个明显的变化：

1. `house_db` 的图标会从 `灰色` 变为 `亮色`（比如从灰褐色变成亮黄色）。
2. 左侧连接树的上方或查询窗口的下方，会显示当前使用的数据库是 `house_db`。

**如果你双击后是想“查看数据库里的内容”，正确的操作是：**

1. **双击选中数据库**：首先双击 `house_db`，确保它的图标变亮。
2. **点击“展开”小三角**：双击不会自动展开子项目。你需要点击数据库名称前面的 **小三角符号 `>`** 来展开它。
3. **查看下属对象**：展开后，你就能看到 `表`、`视图`、`函数` 等分类。再次点击 `表` 前面的小三角，就能看到所有数据表了。这时**双击某个数据表**，才会在右侧打开并显示表中的数据。

**总结一下双击的作用：**

- **双击`连接`**：是连接/断开服务器。
- **双击`数据库`**：是切换/选中当前数据库。
- **双击`数据表`**：是打开并浏览该表的数据。





需要用到小皮吗？为什么双击我的house_db没有反应

“小皮”（PHPStudy）是一个**软件集成环境**，它帮你自动安装和管理了 MySQL、Apache/Nginx、PHP 等软件。

- **如果你的 MySQL 是通过小皮安装的**：
  - 那么**必须**要先启动小皮，并确保里面的 **MySQL 服务是绿色“启动”状态**，Navicat 才能连接上。Navicat 只是一个图形化管理工具，它需要连接到一个正在运行的 MySQL 服务。
  - **操作步骤**：
    1. 打开小皮面板。
    2. 点击 MySQL 旁边的 `启动` 按钮。
    3. 当它显示为绿色并显示“停止”时，表示服务已运行。这时再用 Navicat 进行连接测试。
- **如果你是通过其他方式（如官方安装包）独立安装的 MySQL**：
  - 那么不需要小皮。但需要在 Windows 的“服务”里确保 MySQL 服务是运行状态。（按 `Win + R`，输入 `services.msc`，找到 MySQL 服务并启动它）。

**简单来说：**

小皮是MySQL服务的“开关”和“管家”。Navicat是访问这个服务的“窗口”。你必须先打开“管家”启动服务，才能用“窗口”去访问。

Navicat 就像一个遥控器，MySQL 服务就像电视机。你必须先打开电视机，遥控器才有用。



**如何检查并启动 MySQL 服务？**

**方法一：通过 Windows 服务（最可靠）**

1. 按键盘上的 `Win + R` 键，打开“运行”对话框。
2. 输入 `services.msc`，然后按回车。
3. 会打开“服务”窗口，在列表中找到 MySQL 服务。它的名字可能是：
   - `MySQL`
   - `MySQL80`（如果你安装的是 8.0 版本）
   - `MySQL57`（如果你安装的是 5.7 版本）
4. 查看它的“状态”。
   - **如果状态是“正在运行”**：恭喜，你的服务已经启动，Navicat 可以直接连接。
   - **如果状态是“空白”（已停止）**：你需要手动启动它。
     - 右键点击该服务，选择 `启动`。
     - 为了以后方便，你可以再右键点击它，选择 `属性`，将“启动类型”设置为 `自动`，这样以后开机它就会自己启动。

**方法二：通过命令行（快速检查）**

1. 按 `Win + R`，输入 `cmd`，按回车打开命令提示符。

2. 输入以下命令并回车：

   ```
   mysql -u root -p
   ```

3. 如果系统提示你输入密码，并成功进入 MySQL 命令行界面，说明服务是运行的。

4. 如果出现错误 `Can't connect to MySQL server on 'localhost'`，说明服务没有运行。



二手好房数据库连接问题

将 config.py 中的数据库连接字符串更新为正确的密码：

```
class Config:
    DEBUG = False
    # 使用正确的密码
    SQLALCHEMY_DATABASE_URI = "mysql+pymysql://root:c058836@127.0.0.1/house_db"
    SQLALCHEMY_TRACK_MODIFICATIONS = True
    SQLALCHEMY_POOL_SIZE = 10
    SQLALCHEMY_POOL_TIMEOUT = 30
    SQLALCHEMY_POOL_RECYCLE = 3600
```





**相对导入层级问题**

当直接运行 `python house/app.py` 时：

- `app.py` 成为顶层模块
- `from ..utils` 试图跨越顶层模块，导致错误

```
目录结构
python/
├── house/
│   ├── app.py	# 直接运行这个文件
│   ├── api/
│   │   └── detail.py	# 里面有 from ..utils.linear_model
│   └── utils/
│       └── linear_model.py
```

1.使用绝对导入

```
# 原来的（错误）
from ..utils.linear_model import linear_model_main

# 改为（正确）
from utils.linear_model import linear_model_main
```

2.创建包结构

在 `house` 目录下创建 `__init__.py` 文件，使其成为正式的包：

```
python/
├── house/
│   ├── __init__.py     # 创建这个空文件
│   ├── app.py
│   ├── api/
│   │   ├── __init__.py # 也创建这个
│   │   └── detail.py
│   └── utils/
│       ├── __init__.py # 和这个
│       └── linear_model.py
```











